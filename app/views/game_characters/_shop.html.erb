<div ng-hide="shopHideStatus">
  <ul class='list-group' ng-repeat="shopItem in shopItems" class="list-group">
    <span item-id="shopItem.id" />
    <li class="list-group-item list-group-item-info">
      <span ng-click="itemDetail = !itemDetail" class="item-title">{{ shopItem.name }}  ${{ shopItem.price }}</span>
      <button ng-click="buy(shopItem.id)" class="pull-right btn btn-menu">Buy</button>
      <p>{{ shopItem.description }}</p>
      <div ng-show="itemDetail">
        <p "item-description">{{ shopItem.Strength ? "Strength: " + shopItem.Strength : "" }} {{ shopItem.Constitution ? "Constitution: " + shopItem.Constitution : "" }}</p>
        <img src="{{shopItem.url}}">
      <div class="segment" />
      </div>
    </li>
  </ul>
</div>
<div ng-show="shopHideStatus">
